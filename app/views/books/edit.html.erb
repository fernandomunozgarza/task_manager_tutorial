<%= link_to("Back to Collection", books_path, :class => 'back-link') %>

<div class="books new">
  <h2>Edit Book</h2>

  <%= form_for(@book) do |f| %>
    <% # url: books_path(@book), method: 'patch' %>

    <%= render(partial: 'form', locals: {f: f}) %>


    <div class="form-buttons">
      <%= f.submit("Edit Book") %>
    </div>

  <% end %>
</div>
