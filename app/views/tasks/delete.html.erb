<%= link_to("Back to List", tasks_path, class: 'back-link') %>

<div class="tasks delete">
  <h2>Delete Task</h2>
  <%# needs method: delete to be specified in the form_for helper, otherwise it will default to method: post %>
  <%= form_for(@task, method: 'delete') do |f| %>
    <% # url: task_path(@task), method: 'delete' %>

    <p>Are you sure you want to permanently delete this task?</p>
    <p><%= @task.name %></p>

    <div class="form-buttons">
      <%= f.submit("Delete Task") %>
    </div>

  <% end %>
</div>
